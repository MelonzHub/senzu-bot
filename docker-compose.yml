services:
  senzu-bot:
    build: .
    container_name: senzu-bot
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    mem_limit: 1g
    restart: unless-stopped
    environment:
      - NODE_ENV=production
